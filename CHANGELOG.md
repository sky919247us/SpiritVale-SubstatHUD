# 更新紀錄

本專案採用 [語意化版本](https://semver.org/lang/zh-TW/)。

---

## [1.3.1] - 2026-08-05

> 本版僅文件與封裝調整，**Mod 功能與 v1.3.0 完全相同**。

### 新增
- 便攜版附上 `設定檔範例.cfg`，安裝前就能看到有哪些設定項目
- README 與使用說明加入「關於官方立場」章節 ——
  官方團隊表示允許使用非官方工具，但不提供保證或支援，風險由玩家自負
- README 加入效果展示圖與更新紀錄章節

### 修正
- **設定說明未講清楚 `.cfg` 的生成時機**，導致玩家以為壓縮檔漏檔。
  該檔案是 BepInEx 在插件首次載入時才自動生成的，
  安裝完但尚未啟動過遊戲時，它並不存在。
- 專案檔的 `AssemblyVersion` 一直停在 `1.0.0`，未隨版本更新

---

## [1.3.0] - 2026-08-03

### 新增
- **神器（Artifact）支援**。神器的詞條結構與裝備一致，判定邏輯共用同一套核心。
  神器的詞條設定為全域固定（`Formula.GetArtifactSubstatConfig()` 不需參數）。
- 背包標記同時掛載三個入口：泛型 `Draw(IInfoDrawable, bool)`、裝備專用、神器專用。

### 修正
- 背包星號完全沒出現的問題。背包實際走的是 `Draw(IInfoDrawable, bool)` 泛型入口，
  原本只 patch `Draw(EquipData, bool)` 根本攔不到。
- 所有 `LogDebug` 改為 `LogWarning`。BepInEx 預設的 `LogLevels` 不含 `Debug`，
  導致除錯階段所有例外訊息被靜靜吞掉。

---

## [1.2.0] - 2026-08-03

### 變更
- **星級改以「滿素質」判定** —— 有幾條詞條的實際數值已達區間上限，
  也就是玩家肉眼看到的「滿」。
- 顯示值一律取自 `Formula.GetSubstats()`（遊戲自己算好的結果），
  不再自行用 min/max 內插推算 —— 實測證明內插推不準。
- 總評同時顯示「滿素質 X/Y」與「Roll NN%」兩個指標。
- 新增設定 `以顯示滿值為準`，可切回以內部 roll 百分位判定。

### 修正
- 減益類詞條的判定方向。`MpCost` 這類詞條的區間是 `min=-7, max=-10`（上限比下限小），
  原本的比較式會誤判成滿值。

---

## [1.1.0] - 2026-08-03

### 修正
- **品質判定完全錯誤**的問題。`StatData.Value` 並非詞條數值，
  而是 **0~100 的 roll 百分位**；原本用 `(Value - min) / (max - min)` 計算，
  會算出遠大於 1 的值並被 clamp 成 100%，導致每件裝備都顯示「滿值」。
  正確算法為 `Value / 100`。

### 變更
- 背包標記從「僅上色」改為**名稱前加 ★ 記號**並上色，篩選辨識度大幅提升。
- `自動顯示詞條範圍` 改為**預設關閉** —— 它會與遊戲的按鍵對比功能衝突，
  導致對比視窗每幀重繪而閃爍。

---

## [1.0.0] - 2026-08-02

### 新增
- 首個版本。
- 背包裝備依詞條品質上色。
- 道具說明加入品質總評。
- 道具說明自動顯示詞條區間（免按 ALT）。
